package ru.mirea.task22.taskStep1to3;

public class CalculatorManager {
    Calculator calculator;
    CalculatorWindow window;
    CalculatorManager (Calculator calculator, CalculatorWindow window) {
        this.calculator = calculator;
        this.window = window;
    }
}
